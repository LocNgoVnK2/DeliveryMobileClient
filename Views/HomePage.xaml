<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LoginApp.Maui.Views.HomePage"
             Title="HomePage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackLayout Grid.Row="0">
            <Label 
                Text="Welcome Home!"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />
        </StackLayout>

        <StackLayout Grid.Row="1" Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
            <Button Text="Cần giao" Clicked="OnAboutClicked" />
            <Button Text="Đã nhận" Clicked="OnContactClicked" />
            <Button Text="Đã xong" Clicked="OnContactClicked" />
            <Button Text="Sign Out" Clicked="OnSignOutClicked" />
        </StackLayout>
    </Grid>
</ContentPage>
